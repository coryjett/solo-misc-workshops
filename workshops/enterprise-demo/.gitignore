# Demo-run artifacts — all regenerated by `arctl init` + inline content in
# demo-guide.md. Keeping committed copies is redundant and risks drift, so the
# guide is the single source of truth.
weather-tools/
weather-analysis/
weatherassistant/
weather-assistant-prompt.yaml
agent-deployment.yaml
weather-tools-remote.yaml
